[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[scheduled.functions]]
  name = "ingest-episodes-background"
  cron = "*/30 * * * *"

[functions]
  node_bundler = "esbuild"
